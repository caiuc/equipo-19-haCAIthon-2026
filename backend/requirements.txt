# Versiones verificadas en PyPI el 2026-08-14:
#   fastapi 0.141.1 · uvicorn 0.52.3 · supabase 2.31.0
#   python-dotenv 1.2.2 · email-validator 2.3.0
#
# Sin pines estrictos a proposito: durante la hackathon hay maquinas que ya
# tienen el backend andando. Un pin exacto haria que `pip install -r` las
# actualice y las rompa a mitad del evento.

fastapi
uvicorn[standard]
supabase
python-dotenv
email-validator
